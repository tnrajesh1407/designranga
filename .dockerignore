# Git
.git
.gitignore

# Next.js build output and cache — rebuilt inside the container
.next
out

# Local env — secrets go into Cloud Run env vars, never into the image
.env
.env.*

# Dev tooling
.vercel
.vscode
*.tsbuildinfo

# Test / script helpers
scripts/
coverage/

# OS
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*

# node_modules — reinstalled fresh inside the container for Linux/x64
# (your Windows node_modules has the wrong sharp binary)
node_modules
